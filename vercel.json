{
    "builds": [{
        "src": "the_gram/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "the_gram/wsgi.py"
        }
    ]
}
